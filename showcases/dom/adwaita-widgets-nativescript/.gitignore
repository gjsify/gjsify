# Synced from @gjsify/adwaita-nativescript by `npm run sync:theme`. It used to be a
# COMMITTED hand-copy, and had drifted to 434 lines against the package's 1972 —
# the showcase was demonstrating a stylesheet the package no longer ships.
app/adwaita.css

# NativeScript build artifacts
platforms/
hooks/
.ns-vite-build/
node_modules/
package-lock.json
*.apk
*.aab
