{
    "reviewed": {
        "adw-insensitive": "A styling hook with no look of its own: the applier sets NS_INSENSITIVE_OPACITY on the pencil imperatively, which is what the user sees (entry-row-view.ts, applyEntryRowState).",
        "adw-shortcut-label-keys": "One combination's box. It carries no paint — the 6-unit border-spacing between keycaps rides on the caps themselves via adw-shortcut-label-spaced, because the NS subset has no :first-child.",
        "adw-shortcut-label-disabled": "A consumer hook so the no-shortcut state can be styled without matching on child count. The look comes from the .dimmed half of the same className, which the theme does style.",
        "adw-entry-row": "The row's own base class, and the carrier for the `focused` / `empty` state classes that ride on the same string. Everything libadwaita puts on `row.entry` itself is either unreachable here (a focus-ring, a `:disabled` filter) or already applied: its 6px header margin pairs with the editable area's 6px padding (_lists.scss:186-192) for the same 12 inset `.adw-row` gives every row.",
        "adw-entry-row-suffixes": "The box holding indicator, apply button and pencil. `AdwActionRow.setSuffix` also stamps `.adw-row-suffix`, which places and vertically centres it, and libadwaita puts no spacing on the box either — the 6 between the three rides on the children (_lists.scss:200-203).",
        "adw-password-entry-row": "A subclass marker. C adds a bare `password` class to the row (adw-password-entry-row.c:173); every rule that selects it lands on descendants this port styles by their own class — the caps-lock dim is `> .indicator`, which `.adw-entry-indicator` now carries.",
        "adw-password-peek": "The reveal/conceal toggle. C builds it `flat` (adw-password-entry-row.c:151), which is what `.adw-image-button` already is; the icon it swaps between is a bitmap the widget renders, not something a rule could pick.",
        "adw-menu-button": "A marker on an AdwImageButton subclass. `Adw.MenuButton` in a header bar IS a flat icon button, so the base class carries the whole look; the class exists so an app can reach its own menu buttons.",
        "adw-preferences-group-labels": "The title + description column. libadwaita gives `box.labels` one property, `border-spacing: 6px` (_preferences.scss:7-9), and the NS subset has no spacing — so the 6 rides on `.adw-preferences-group-description`'s top margin. Its own placement is the grid column and `verticalAlignment`, both set in the widget.",
        "adw-preferences-group-suffix": "A wrapper this port invented to place the header suffix in grid column 1 — libadwaita adds the suffix straight to `header_box` (adw-preferences-group.c:598-617) and puts no spacing on that box. The column and the middle alignment are set in the widget, so there is nothing left for a rule to say."
    },
    "unreviewedBaseline": [
        "adw-bottom-sheet",
        "adw-bottom-sheet-content",
        "adw-bottom-sheet-sheet",
        "adw-carousel",
        "adw-carousel-page",
        "adw-carousel-track",
        "adw-expander-row",
        "adw-icon",
        "adw-inline-view-switcher",
        "adw-spin-minus",
        "adw-spin-plus",
        "adw-spin-row",
        "adw-spinner-ring",
        "adw-split-button-action-icon",
        "adw-split-button-chevron",
        "adw-switch-row",
        "adw-tab-view",
        "adw-toast-overlay",
        "adw-toast-overlay-content",
        "adw-wrap-box"
    ]
}
