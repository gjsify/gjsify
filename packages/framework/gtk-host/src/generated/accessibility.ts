// GENERATED by src/generator/main.mts — do not edit.
//
// Provenance: Gtk-4.0/4.23.3 Adw-1/1.10.0
//
// GTK's ARIA surface: 53 slots over three tables, and the props interface
// that types them. The one generated table the runtime CANNOT re-derive from the object
// in front of it — see `emitAccessibility` in src/generator/emit-types.mts.

import type Gtk from '@girs/gtk-4.0';

import type { AriaSlot } from '../types.js';
import type {
    GtkAccessibleAutocompleteNick,
    GtkAccessibleInvalidStateNick,
    GtkAccessibleSortNick,
    GtkAccessibleTristateNick,
    GtkOrientationNick,
} from './props.js';

/**
 * ARIA name -> the table it belongs to and the kind of value GTK collects for it.
 *
 * Keyed by the NAME alone: across the three tables of this GTK the names are distinct,
 * and `girs-vocabulary.mts` fails the generation rather than emit a map that silently
 * kept one of two. The table name is what the runtime derives its GTK call from.
 */
export const ARIA_SLOTS: Readonly<Record<string, AriaSlot>> = {
    'active-descendant': { table: 'GtkAccessibleRelation', kind: 'reference' },
    autocomplete: { table: 'GtkAccessibleProperty', kind: 'enum', enumGType: 'GtkAccessibleAutocomplete' },
    busy: { table: 'GtkAccessibleState', kind: 'boolean' },
    checked: { table: 'GtkAccessibleState', kind: 'enum', enumGType: 'GtkAccessibleTristate' },
    'col-count': { table: 'GtkAccessibleRelation', kind: 'integer' },
    'col-index': { table: 'GtkAccessibleRelation', kind: 'integer' },
    'col-index-text': { table: 'GtkAccessibleRelation', kind: 'string' },
    'col-span': { table: 'GtkAccessibleRelation', kind: 'integer' },
    'controlled-by': { table: 'GtkAccessibleRelation', kind: 'reference' },
    controls: { table: 'GtkAccessibleRelation', kind: 'reference' },
    'described-by': { table: 'GtkAccessibleRelation', kind: 'reference' },
    description: { table: 'GtkAccessibleProperty', kind: 'string' },
    'description-for': { table: 'GtkAccessibleRelation', kind: 'reference' },
    details: { table: 'GtkAccessibleRelation', kind: 'reference' },
    'details-for': { table: 'GtkAccessibleRelation', kind: 'reference' },
    disabled: { table: 'GtkAccessibleState', kind: 'boolean' },
    'error-message': { table: 'GtkAccessibleRelation', kind: 'reference' },
    'error-message-for': { table: 'GtkAccessibleRelation', kind: 'reference' },
    expanded: { table: 'GtkAccessibleState', kind: 'boolean' },
    'flow-from': { table: 'GtkAccessibleRelation', kind: 'reference' },
    'flow-to': { table: 'GtkAccessibleRelation', kind: 'reference' },
    'has-popup': { table: 'GtkAccessibleProperty', kind: 'boolean' },
    'help-text': { table: 'GtkAccessibleProperty', kind: 'string' },
    hidden: { table: 'GtkAccessibleState', kind: 'boolean' },
    invalid: { table: 'GtkAccessibleState', kind: 'enum', enumGType: 'GtkAccessibleInvalidState' },
    'key-shortcuts': { table: 'GtkAccessibleProperty', kind: 'string' },
    label: { table: 'GtkAccessibleProperty', kind: 'string' },
    'label-for': { table: 'GtkAccessibleRelation', kind: 'reference' },
    'labelled-by': { table: 'GtkAccessibleRelation', kind: 'reference' },
    level: { table: 'GtkAccessibleProperty', kind: 'integer' },
    modal: { table: 'GtkAccessibleProperty', kind: 'boolean' },
    'multi-line': { table: 'GtkAccessibleProperty', kind: 'boolean' },
    'multi-selectable': { table: 'GtkAccessibleProperty', kind: 'boolean' },
    orientation: { table: 'GtkAccessibleProperty', kind: 'enum', enumGType: 'GtkOrientation' },
    owns: { table: 'GtkAccessibleRelation', kind: 'reference' },
    placeholder: { table: 'GtkAccessibleProperty', kind: 'string' },
    'pos-in-set': { table: 'GtkAccessibleRelation', kind: 'integer' },
    pressed: { table: 'GtkAccessibleState', kind: 'enum', enumGType: 'GtkAccessibleTristate' },
    'read-only': { table: 'GtkAccessibleProperty', kind: 'boolean' },
    required: { table: 'GtkAccessibleProperty', kind: 'boolean' },
    'role-description': { table: 'GtkAccessibleProperty', kind: 'string' },
    'row-count': { table: 'GtkAccessibleRelation', kind: 'integer' },
    'row-index': { table: 'GtkAccessibleRelation', kind: 'integer' },
    'row-index-text': { table: 'GtkAccessibleRelation', kind: 'string' },
    'row-span': { table: 'GtkAccessibleRelation', kind: 'integer' },
    selected: { table: 'GtkAccessibleState', kind: 'boolean' },
    'set-size': { table: 'GtkAccessibleRelation', kind: 'integer' },
    sort: { table: 'GtkAccessibleProperty', kind: 'enum', enumGType: 'GtkAccessibleSort' },
    'value-max': { table: 'GtkAccessibleProperty', kind: 'double' },
    'value-min': { table: 'GtkAccessibleProperty', kind: 'double' },
    'value-now': { table: 'GtkAccessibleProperty', kind: 'double' },
    'value-text': { table: 'GtkAccessibleProperty', kind: 'string' },
    visited: { table: 'GtkAccessibleState', kind: 'boolean' },
};

/**
 * What `accessibility={{ … }}` accepts.
 *
 * ONE OBJECT PROP, NOT 53 FLAT ONES, and the decision is measured rather than
 * stylistic. Measured in this repo's own type surface: TypeScript exempts every
 * hyphen-containing JSX attribute from excess-property checking, so a flat `aria-labell`
 * would be accepted in silence, while a key inside a FRESH object literal is checked —
 * `accessibility={{ labell: 'x' }}` is TS2561, naming the key and suggesting `label`. A grouped
 * prop is also the spelling the other surface over this same vocabulary has: a `.blp` author
 * writes `accessibility { label: "…"; }`, and ADR 0034 exists so two surfaces over one
 * vocabulary do not invent two spellings for one name.
 *
 * KEBAB, THE SPELLING GTK ITSELF USES. A second camelCase spelling would double the
 * surface for nothing: unlike a JSX attribute, a quoted key in an object literal IS
 * excess-property checked, so the one spelling already catches the typo.
 *
 * `null` clears a slot, as it does for every property on this host — `gtk_accessible_reset_*`
 * is what GTK offers and what the host calls.
 */
export interface AccessibilityAttributes {
    /**
     * `GtkAccessibleRelation.active-descendant` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'active-descendant'?: never;

    /** `GtkAccessibleProperty.autocomplete` — a GtkAccessibleAutocomplete, which GTK collects as an int: the nick or the constant. */
    autocomplete?: GtkAccessibleAutocompleteNick | Gtk.AccessibleAutocomplete | null;

    /** `GtkAccessibleState.busy` — GTK collects it as a boolean. */
    busy?: boolean | null;

    /** `GtkAccessibleState.checked` — a GtkAccessibleTristate, which GTK collects as an int: the nick or the constant. */
    checked?: GtkAccessibleTristateNick | Gtk.AccessibleTristate | null;

    /** `GtkAccessibleRelation.col-count` — GTK collects it as an integer. */
    'col-count'?: number | null;

    /** `GtkAccessibleRelation.col-index` — GTK collects it as an integer. */
    'col-index'?: number | null;

    /** `GtkAccessibleRelation.col-index-text` — GTK collects it as a string. */
    'col-index-text'?: string | null;

    /** `GtkAccessibleRelation.col-span` — GTK collects it as an integer. */
    'col-span'?: number | null;

    /**
     * `GtkAccessibleRelation.controlled-by` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'controlled-by'?: never;

    /**
     * `GtkAccessibleRelation.controls` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    controls?: never;

    /**
     * `GtkAccessibleRelation.described-by` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'described-by'?: never;

    /** `GtkAccessibleProperty.description` — GTK collects it as a string. */
    description?: string | null;

    /**
     * `GtkAccessibleRelation.description-for` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'description-for'?: never;

    /**
     * `GtkAccessibleRelation.details` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    details?: never;

    /**
     * `GtkAccessibleRelation.details-for` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'details-for'?: never;

    /** `GtkAccessibleState.disabled` — GTK collects it as a boolean. */
    disabled?: boolean | null;

    /**
     * `GtkAccessibleRelation.error-message` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'error-message'?: never;

    /**
     * `GtkAccessibleRelation.error-message-for` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'error-message-for'?: never;

    /** `GtkAccessibleState.expanded` — GTK collects it as a boolean. */
    expanded?: boolean | null;

    /**
     * `GtkAccessibleRelation.flow-from` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'flow-from'?: never;

    /**
     * `GtkAccessibleRelation.flow-to` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'flow-to'?: never;

    /** `GtkAccessibleProperty.has-popup` — GTK collects it as a boolean. */
    'has-popup'?: boolean | null;

    /** `GtkAccessibleProperty.help-text` — GTK collects it as a string. */
    'help-text'?: string | null;

    /** `GtkAccessibleState.hidden` — GTK collects it as a boolean. */
    hidden?: boolean | null;

    /** `GtkAccessibleState.invalid` — a GtkAccessibleInvalidState, which GTK collects as an int: the nick or the constant. */
    invalid?: GtkAccessibleInvalidStateNick | Gtk.AccessibleInvalidState | null;

    /** `GtkAccessibleProperty.key-shortcuts` — GTK collects it as a string. */
    'key-shortcuts'?: string | null;

    /** `GtkAccessibleProperty.label` — GTK collects it as a string. */
    label?: string | null;

    /**
     * `GtkAccessibleRelation.label-for` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'label-for'?: never;

    /**
     * `GtkAccessibleRelation.labelled-by` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    'labelled-by'?: never;

    /** `GtkAccessibleProperty.level` — GTK collects it as an integer. */
    level?: number | null;

    /** `GtkAccessibleProperty.modal` — GTK collects it as a boolean. */
    modal?: boolean | null;

    /** `GtkAccessibleProperty.multi-line` — GTK collects it as a boolean. */
    'multi-line'?: boolean | null;

    /** `GtkAccessibleProperty.multi-selectable` — GTK collects it as a boolean. */
    'multi-selectable'?: boolean | null;

    /** `GtkAccessibleProperty.orientation` — a GtkOrientation, which GTK collects as an int: the nick or the constant. */
    orientation?: GtkOrientationNick | Gtk.Orientation | null;

    /**
     * `GtkAccessibleRelation.owns` — a REFERENCE to another widget, and this host has no way
     * to name one. There is no `id` prop and `ref` is resolved by the framework after the
     * props are applied, so the value would be `null` on the render that authors it. Declared
     * and unwritable rather than absent: GTK has this name, the gap is ours.
     */
    owns?: never;

    /** `GtkAccessibleProperty.placeholder` — GTK collects it as a string. */
    placeholder?: string | null;

    /** `GtkAccessibleRelation.pos-in-set` — GTK collects it as an integer. */
    'pos-in-set'?: number | null;

    /** `GtkAccessibleState.pressed` — a GtkAccessibleTristate, which GTK collects as an int: the nick or the constant. */
    pressed?: GtkAccessibleTristateNick | Gtk.AccessibleTristate | null;

    /** `GtkAccessibleProperty.read-only` — GTK collects it as a boolean. */
    'read-only'?: boolean | null;

    /** `GtkAccessibleProperty.required` — GTK collects it as a boolean. */
    required?: boolean | null;

    /** `GtkAccessibleProperty.role-description` — GTK collects it as a string. */
    'role-description'?: string | null;

    /** `GtkAccessibleRelation.row-count` — GTK collects it as an integer. */
    'row-count'?: number | null;

    /** `GtkAccessibleRelation.row-index` — GTK collects it as an integer. */
    'row-index'?: number | null;

    /** `GtkAccessibleRelation.row-index-text` — GTK collects it as a string. */
    'row-index-text'?: string | null;

    /** `GtkAccessibleRelation.row-span` — GTK collects it as an integer. */
    'row-span'?: number | null;

    /** `GtkAccessibleState.selected` — GTK collects it as a boolean. */
    selected?: boolean | null;

    /** `GtkAccessibleRelation.set-size` — GTK collects it as an integer. */
    'set-size'?: number | null;

    /** `GtkAccessibleProperty.sort` — a GtkAccessibleSort, which GTK collects as an int: the nick or the constant. */
    sort?: GtkAccessibleSortNick | Gtk.AccessibleSort | null;

    /** `GtkAccessibleProperty.value-max` — GTK collects it as a double. */
    'value-max'?: number | null;

    /** `GtkAccessibleProperty.value-min` — GTK collects it as a double. */
    'value-min'?: number | null;

    /** `GtkAccessibleProperty.value-now` — GTK collects it as a double. */
    'value-now'?: number | null;

    /** `GtkAccessibleProperty.value-text` — GTK collects it as a string. */
    'value-text'?: string | null;

    /** `GtkAccessibleState.visited` — GTK collects it as a boolean. */
    visited?: boolean | null;
}
