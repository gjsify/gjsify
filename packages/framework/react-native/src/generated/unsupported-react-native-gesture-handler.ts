// GENERATED by scripts/generate-exports.mjs — do not edit.
//
// One export per react-native-gesture-handler name this layer does not answer yet, each a value
// that refuses with the support table’s own sentence. Why these exist at all, and
// why they are generated rather than looped: see the generator and `unsupported.ts`.
//
// The MODULE is passed with every name (ADR 0036): `StatusBar` is a react-native
// export and the whole of expo-status-bar, `Image` is react-native’s and
// expo-image’s — without it the one-argument lookup answers from whichever surface
// the registry lists first, so a planned name would report another surface’s
// "is available".

import { unsupported } from '../unsupported.js';

export const Gesture = unsupported('Gesture', 'react-native-gesture-handler');
export const GestureDetector = unsupported('GestureDetector', 'react-native-gesture-handler');
export const PanGestureHandler = unsupported('PanGestureHandler', 'react-native-gesture-handler');
export const TapGestureHandler = unsupported('TapGestureHandler', 'react-native-gesture-handler');
export const LongPressGestureHandler = unsupported('LongPressGestureHandler', 'react-native-gesture-handler');
export const PinchGestureHandler = unsupported('PinchGestureHandler', 'react-native-gesture-handler');
export const Swipeable = unsupported('Swipeable', 'react-native-gesture-handler');
export const ScrollView = unsupported('ScrollView', 'react-native-gesture-handler');
export const Directions = unsupported('Directions', 'react-native-gesture-handler');
export const State = unsupported('State', 'react-native-gesture-handler');
