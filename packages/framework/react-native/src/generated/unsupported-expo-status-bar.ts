// GENERATED by scripts/generate-exports.mjs — do not edit.
//
// One export per expo-status-bar name this layer does not answer yet, each a value
// that refuses with the support table’s own sentence. Why these exist at all, and
// why they are generated rather than looped: see the generator and `unsupported.ts`.
//
// The MODULE is passed with every name (ADR 0036): `StatusBar` is a react-native
// export and the whole of expo-status-bar, `Image` is react-native’s and
// expo-image’s — without it the one-argument lookup answers from whichever surface
// the registry lists first, so a planned name would report another surface’s
// "is available".

import { unsupported } from '../unsupported.js';

export const setStatusBarStyle = unsupported('setStatusBarStyle', 'expo-status-bar');
export const setStatusBarHidden = unsupported('setStatusBarHidden', 'expo-status-bar');
export const setStatusBarBackgroundColor = unsupported('setStatusBarBackgroundColor', 'expo-status-bar');
export const setStatusBarTranslucent = unsupported('setStatusBarTranslucent', 'expo-status-bar');
export const setStatusBarNetworkActivityIndicatorVisible = unsupported('setStatusBarNetworkActivityIndicatorVisible', 'expo-status-bar');
