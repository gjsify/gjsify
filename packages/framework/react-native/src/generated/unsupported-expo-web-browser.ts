// GENERATED by scripts/generate-exports.mjs — do not edit.
//
// One export per expo-web-browser name this layer does not answer yet, each a value
// that refuses with the support table’s own sentence. Why these exist at all, and
// why they are generated rather than looped: see the generator and `unsupported.ts`.
//
// The MODULE is passed with every name (ADR 0036): `StatusBar` is a react-native
// export and the whole of expo-status-bar, `Image` is react-native’s and
// expo-image’s — without it the one-argument lookup answers from whichever surface
// the registry lists first, so a planned name would report another surface’s
// "is available".

import { unsupported } from '../unsupported.js';

export const dismissBrowser = unsupported('dismissBrowser', 'expo-web-browser');
export const openAuthSessionAsync = unsupported('openAuthSessionAsync', 'expo-web-browser');
export const dismissAuthSession = unsupported('dismissAuthSession', 'expo-web-browser');
export const maybeCompleteAuthSession = unsupported('maybeCompleteAuthSession', 'expo-web-browser');
export const warmUpAsync = unsupported('warmUpAsync', 'expo-web-browser');
export const coolDownAsync = unsupported('coolDownAsync', 'expo-web-browser');
