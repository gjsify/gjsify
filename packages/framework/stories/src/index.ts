// @gjsify/stories — runtime-agnostic story-authoring contract.
// Pure named exports, zero platform imports. Consumed by @gjsify/storybook
// (the GTK renderer) and any future web / nativescript renderer.

export * from './types.js';
export * from './args.js';
