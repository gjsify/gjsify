using Gtk 4.0;
using Adw 1;

Adw.ToolbarView {
  [top]
  $GalleryHeaderBar {
  }

  content: $GalleryToolbarView pane {
    Gtk.Label {
      label: "a real child of an extern parent";
    }

    $Ns.Inner {
    }
  };
}
