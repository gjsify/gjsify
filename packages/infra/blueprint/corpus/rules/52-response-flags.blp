using Gtk 4.0;
using Adw 1;

Adw.AlertDialog {
  heading: "confirm";

  responses [
    plain: "Plain",
    discard: _("Discard") destructive,
    save: C_("verb", "Save") suggested disabled,
    later: "Later" disabled,
  ]
}
