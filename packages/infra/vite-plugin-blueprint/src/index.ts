// Skeleton — implementation lands in commit 2 (port from external gjsify/vite repo).

export interface BlueprintPluginOptions {
    minify?: boolean;
}

export function blueprintPlugin(_options: BlueprintPluginOptions = {}): unknown {
    throw new Error('@gjsify/vite-plugin-blueprint: not implemented yet');
}

export default blueprintPlugin;
