---
title: "Fireworks (Canvas 2D)"
description: Particle fireworks drawn with the Canvas 2D API, running in a GTK 4 window and in the browser from one shared file.
---

import ShowcaseEmbed from '../../../components/ShowcaseEmbed.astro';

Animated particle fireworks drawn with the plain Canvas 2D API. Click anywhere to fire one. The demo shows that `<canvas>` code you already know draws into a native GTK 4 window without a single gjsify import in the drawing code, so a browser animation you like can become a desktop app as-is.

Adapted from [a CodePen by @juliangarnier](https://codepen.io/juliangarnier/pen/gmOwJX).

## Live demo

<ShowcaseEmbed name="canvas2d-fireworks" />

## Run it

This showcase declares all four runtimes, so pick the one you already have:

```bash
gjsify showcase canvas2d-fireworks --runtime gjs
gjsify showcase canvas2d-fireworks --runtime node
gjsify showcase canvas2d-fireworks --runtime bun
gjsify showcase canvas2d-fireworks --runtime deno
```

A GTK 4 window opens with the canvas on the right and an Adwaita sidebar on the left. Click the canvas to fire a firework. The sidebar rows adjust particle count, auto-fire interval, maximum burst radius, and whether fireworks fire on their own.

The animation ticks on the GTK frame clock, so `requestAnimationFrame` lands on vsync exactly as it does in a browser.

Under `gjs` this runs the `--app gjs` bundle against GJS's native `gi://`. Under `node`, `bun` and `deno` it runs the `--app node` bundle, where `gi://` goes through the [node-gi](/gjsify/projects/node-gi/) bridge. Drop the flag and the CLI runs the `--app gjs` bundle when `gjs` is on PATH, otherwise the `--app node` bundle on the runtime it is already on.

This is also the showcase to reach for when you want to see [devtools](/gjsify/guides/devtools/) at work. Start it with `GJSIFY_DEVTOOLS=1` and you get the same `GetStatus`, `DumpTree` and `Screenshot` D-Bus calls on all four runtimes.

## Run it in the browser

```ts
import { mount } from '@gjsify/example-dom-canvas2d-fireworks/browser';

const handle = mount(document.getElementById('app')!);
handle.pause();
handle.resume();
```

Same code, except the `<canvas>` is a real browser canvas rather than a GTK widget.

## What it uses

| Package | Surface used |
|---|---|
| [`@gjsify/canvas2d`](https://github.com/gjsify/gjsify/tree/main/packages/framework/canvas2d) | `Canvas2DBridge`, a `Gtk.DrawingArea` that draws with Cairo and PangoCairo |
| [`@gjsify/dom-elements`](https://github.com/gjsify/gjsify/tree/main/packages/dom/dom-elements) | `HTMLCanvasElement`, `getBoundingClientRect()`, `requestAnimationFrame` |
| [`@gjsify/event-bridge`](https://github.com/gjsify/gjsify/tree/main/packages/framework/event-bridge) | GTK 4 pointer input arriving as a DOM `mousedown` event |
| `@gjsify/adwaita-web` | The Adwaita control panel in the browser build |

You do not import any of them. The animation calls `ctx.beginPath()`, `ctx.arc()`, `ctx.fill()`, `ctx.stroke()` and `requestAnimationFrame()`, and the build wires those to GTK 4 and Cairo.

## Copy the structure

[`showcases/dom/canvas2d-fireworks/`](https://github.com/gjsify/gjsify/tree/main/showcases/dom/canvas2d-fireworks)

- `src/fireworks.ts` is the whole simulation behind one `start(canvas)` function. No platform imports.
- `src/gjs/gjs.ts` plus `fireworks-window.blp` are the `Adw.Application` shell and its Blueprint UI.
- `src/browser/browser.ts` exports `mount()`; `browser-main.ts` is the standalone full-page entry.

Put your app logic in the middle file and you get both targets for the cost of two small shells.

## Related

- [Bridge widgets](/gjsify/patterns/bridges/) covers why `Canvas2DBridge` is itself a `Gtk.DrawingArea`, and the `onReady()` plus `installGlobals()` lifecycle.
- [`excalibur-jelly-jumper`](../excalibur-jelly-jumper/) puts a whole game engine on the same bridges.
