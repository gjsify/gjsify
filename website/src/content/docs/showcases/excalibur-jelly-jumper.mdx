---
title: "Jelly Jumper (Excalibur)"
description: A 2D platformer built on the unmodified Excalibur.js engine, running in a GTK 4 window on GJS, Node, Bun and Deno, and in the browser.
---

import ShowcaseEmbed from '../../../components/ShowcaseEmbed.astro';

A 2D platformer built with [Excalibur.js](https://excaliburjs.com/) 0.32 and [Tiled](https://www.mapeditor.org/) tilemaps. The engine comes straight from npm with no fork and no patch.

That is the thing to take away: a game engine written for the browser, reaching for WebGL2, `<canvas>`, `XMLHttpRequest`, `DOMParser`, `ResizeObserver`, `requestAnimationFrame` and WebAudio, runs inside a native Adwaita window. If gjsify can carry Excalibur, it can carry the npm library you had in mind.

Based on [sample-jelly-jumper](https://github.com/excaliburjs/sample-jelly-jumper) by the Excalibur team.

## Live demo

<ShowcaseEmbed name="excalibur-jelly-jumper" />

## Run it

This showcase declares all four runtimes, so pick the one you already have:

```bash
gjsify showcase excalibur-jelly-jumper --runtime gjs
gjsify showcase excalibur-jelly-jumper --runtime node
gjsify showcase excalibur-jelly-jumper --runtime bun
gjsify showcase excalibur-jelly-jumper --runtime deno
```

A GTK 4 window opens with the game filling it and pause and mute buttons in the header bar. Same window and same game on all four.

| Action | Keyboard | Gamepad |
|---|---|---|
| Move | Arrow keys | D-pad |
| Jump | `A` | Face button 1 (A on an Xbox pad) |
| Run | `S` | Face button 3 (X on an Xbox pad) |
| Performance overlay | `F1` | |

The engine renders through WebGL2 on `Gtk.GLArea`. If no GL2 context is available it falls back to Canvas 2D over Cairo, the same fallback the browser build takes.

Under `gjs` the game runs the `--app gjs` bundle against GJS's native `gi://`. Under `node`, `bun` and `deno` it runs one shared `--app node` bundle, where `gi://` goes through the [node-gi](/gjsify/projects/node-gi/) bridge. [Choose the runtime](/gjsify/showcases/#choose-the-runtime) covers what the CLI does when you leave the flag off.

## Run it in the browser

```ts
import { mount } from '@gjsify/example-dom-excalibur-jelly-jumper/browser';

const handle = mount(document.getElementById('app')!, {
  assetBase: '/demos/jelly-jumper/',   // where the tilemaps and spritesheets live
  startMuted: true,
});

handle.unmute();
handle.pause();
```

`assetBase` is what makes one resource table serve both targets: the game's `/res/...` paths are rewritten against it at load time. The handle also exposes `resume()`, `mute()`, `isPaused` and `isMuted`.

Tilemaps and spritesheets ship with the package under `@gjsify/example-dom-excalibur-jelly-jumper/assets/*`.

## What it uses

| Package | What Excalibur needs it for |
|---|---|
| [`@gjsify/webgl`](https://github.com/gjsify/gjsify/tree/main/packages/framework/webgl) | `WebGLBridge` on `Gtk.GLArea`, Excalibur's WebGL2 renderer |
| [`@gjsify/canvas2d`](https://github.com/gjsify/gjsify/tree/main/packages/framework/canvas2d) | The Cairo fallback renderer when GL2 is unavailable |
| [`@gjsify/dom-elements`](https://github.com/gjsify/gjsify/tree/main/packages/dom/dom-elements) | `HTMLCanvasElement` and the `ResizeObserver` that `DisplayMode.FitContainerAndFill` watches |
| [`@gjsify/xmlhttprequest`](https://github.com/gjsify/gjsify/tree/main/packages/web/xmlhttprequest) and [`@gjsify/fetch`](https://github.com/gjsify/gjsify/tree/main/packages/web/fetch) | The asset loader, including `arraybuffer` and `blob` responses |
| [`@gjsify/domparser`](https://github.com/gjsify/gjsify/tree/main/packages/web/domparser) | `@excaliburjs/plugin-tiled` parsing `.tmx` map files |
| [`@gjsify/event-bridge`](https://github.com/gjsify/gjsify/tree/main/packages/framework/event-bridge) | Keyboard and gamepad input |
| [`@gjsify/webaudio`](https://github.com/gjsify/gjsify/tree/main/packages/web/webaudio) | Sound effects, played through GStreamer in the GTK build |

## Copy the structure

[`showcases/dom/excalibur-jelly-jumper/`](https://github.com/gjsify/gjsify/tree/main/showcases/dom/excalibur-jelly-jumper)

Every target enters the same `startGame(canvas, options)` in `src/game.ts`. The scenes, actors, physics, components, state and UI all sit at the top level of `src/` and never mention a platform. Only the shell differs: `src/gjs/gjs.ts` builds the `Adw.Application` and its window, `src/browser/browser.ts` exports `mount()`, and `src/browser/browser-main.ts` runs it full-page.

Per-target tuning goes through options on that one function (`pixelRatio`, `fixedUpdateFps`, `enablePerf`) rather than through forked code.

## Related

- [Bridge widgets](/gjsify/patterns/bridges/) covers the `WebGLBridge` lifecycle this game sits on, including how resizes reach a `ResizeObserver`.
- [`canvas2d-fireworks`](../canvas2d-fireworks/) is the smaller demo on the same bridges.
- [`pixel-rpg/map-editor`](https://github.com/PixelRPG/map-editor) is a downstream project built on the same Excalibur and Tiled stack.
