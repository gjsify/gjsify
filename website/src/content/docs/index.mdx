---
title: GJSify
description: The TypeScript framework for native apps — GTK 4 / Adwaita on GJS, Node.js, Deno and Bun, across Linux, macOS and Windows
template: splash
hero:
  tagline: Native apps, in TypeScript
---

import PlatformStage from '../../components/PlatformStage.astro';
import QuickStartCta from '../../components/QuickStartCta.astro';

{/* The home page stays deliberately minimal: the hero + showcase slideshow
    (rendered by the custom Hero override) already tell the story, then the one
    claim the slideshow cannot make on its own — the same app on three desktops —
    followed by a single "get started" call-to-action. The deeper material — what
    makes GJSify different, the coverage dashboards, the bridge widgets — lives in
    the documentation subpages (Overview, Coverage, Patterns/Bridges). */}

<PlatformStage />

<QuickStartCta />
