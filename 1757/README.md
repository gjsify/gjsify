# PR #1757 — the dark active toggle is grey

- `1.png` — dark mode, three rows: **web before** (red, solid white pill), **web after**
  (green, translucent grey pill), **GTK reference** (red).

Captured 2026-09-22, story `Buttons/Toggle Group`. GTK via the devtools D-Bus `Screenshot`
with a widget path from `FindWidget`; web via Playwright/Firefox element screenshot.
